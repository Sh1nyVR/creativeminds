{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --include=dev --workspaces --include-workspace-root && npm install --include=dev --workspace=@scratch/scratch-gui",
  "buildCommand": "node -e 'const fs=require(\"fs\");const path=require(\"path\");const cp=require(\"child_process\");const cwd=process.cwd();const pkgPath=path.join(cwd,\"package.json\");const pkg=JSON.parse(fs.readFileSync(pkgPath,\"utf8\"));const isMonorepoRoot=Array.isArray(pkg.workspaces);if(isMonorepoRoot){cp.execSync(\"npm run build:dev --workspace=@scratch/scratch-gui\",{stdio:\"inherit\"});const src=path.join(cwd,\"packages\",\"scratch-gui\",\"build\");const dst=path.join(cwd,\"build\");if(!fs.existsSync(src)){throw new Error(\"Missing source output: \"+src);}fs.rmSync(dst,{recursive:true,force:true});fs.cpSync(src,dst,{recursive:true});console.log(\"Prepared output:\",dst);}else{cp.execSync(\"npm run build:dev\",{stdio:\"inherit\"});const dst=path.join(cwd,\"build\");if(!fs.existsSync(dst)){throw new Error(\"Missing output: \"+dst);}console.log(\"Prepared output:\",dst);}'",
  "outputDirectory": "build",
  "functions": {
    "api/ov.js": {
      "runtime": "nodejs20.x",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/ov/:path*",
      "destination": "/api/ov?path=:path*"
    }
  ]
}
